export const MEGABYTE = 1048576; // in bytes

// maximum size of a single uploaded file
export const MAX_FILE_SIZE = 25 * MEGABYTE;

// maximum size of the whole upload batch (folder/directory)
export const MAX_TOTAL_SIZE = 100 * MEGABYTE;

export const IPFS_DEFAULT_GATEWAY = process.env.NEXT_IPFS_DEFAULT_GATEWAY;

